# دليل الإعداد — linux-blog

> هذا الملف موجّه لك أنت (صاحب الموقع). مكتوب بالعربية وبخطوات مفصّلة.
> يُحدَّث بعد كل مرحلة. المراحل 4 و5 و6 خلصوا كلهم — فيه خطوات يدوية بسيطة
> ومطلوبة منك، اقرأ قسم "المراحل 4-5-6" تحت الأول.

---

## ✅ ما الذي تم في المرحلة 1

- مشروع **Next.js 16 + TypeScript + Tailwind CSS v4** جاهز.
- دعم لغتين: **الإنجليزية (LTR) هي الافتراضية** + **العربية (RTL)** عبر مكتبة `next-intl`.
- تبديل بين اللغتين من زر في الشريط العلوي.
- **الوضع الليلي (Dark) هو الافتراضي** + زر تبديل للوضع الفاتح، والتفضيل يُحفظ ولا يحدث "وميض" عند فتح الصفحة.
- خطوط: **IBM Plex Sans Arabic** للعربية، **Inter** للإنجليزية، **JetBrains Mono** للأكواد.
- شريط علوي (Header) وشريط سفلي (Footer) وصفحة رئيسية بتصميم مستوحى من الطرفية.
- **أيقونة احترافية للموقع** + الموقع **قابل للتثبيت كتطبيق (PWA)** على الهاتف والحاسوب.

---

## ✅ ما الذي تم في المرحلة 2

- **المدونة شغالة فعلياً**: المقالات ملفات Markdown عادية (`.md`) في مجلد
  `content/articles/`، بالعربي والإنجليزي، وعليها 6 مقالات تجريبية (3 لكل لغة)
  عشان تشوف الشكل النهائي.
- صفحة **`/articles`** (قائمة كل المقالات) بفلتر حسب التصنيف (tag).
- صفحة كل مقال فيها: **جدول محتويات**، **وقت القراءة**، تلوين الأكواد (بلوكات
  الكود دايماً LTR حتى في صفحة عربية)، زرار **نسخ الكود**، روابط
  «المقال السابق/التالي»، ومقالات ذات صلة تلقائياً حسب التصنيفات.
- صفحة **`/about`** (عنّي).
- خلاصة **RSS** على `/rss.xml`.
- الصفحة الرئيسية بقت تعرض أحدث المقالات فعلياً.

### ✍️ إزاي تنشر مقال جديد بنفسك

الشرح الكامل مع مثال frontmatter موجود في **[README.md](./README.md)** (قسم
"Adding a new article"). ملخص سريع:

1. اعمل ملف جديد في `content/articles/en/اسم-المقال.md` (أو `content/articles/ar/`
   للعربي).
2. اكتب في أوله بيانات المقال (العنوان، الوصف، التاريخ، التصنيفات) ثم المحتوى
   بصيغة Markdown عادية تحته.
3. خليه `draft: true` وانت لسه بتكتب (يظهر عندك محلياً بس، مش على الموقع الحي).
   لما يخلص، غيّرها لـ `draft: false`.
4. `git add` ثم `git commit` ثم `git push` — Vercel هينشره تلقائياً.

الصفحات الباقية (الأسئلة، تسجيل الدخول، لوحة الأدمن...) هتيجي في المراحل التالية.

---

## 🔄 تحديث بعد المرحلة 2 (بناءً على طلبك)

- **واجهة الموقع بقت إنجليزي بس** (مفيش زرار تبديل لغة، ومفيش `/ar` أو `/en` في الرابط
  خالص). لكن **المقال نفسه** ممكن يكون عربي أو إنجليزي — بتحدد ده من جوه الملف
  (`locale: en` أو `locale: ar`)، والمقال العربي بيتعرض RTL تلقائياً (مع بادچ صغير
  "AR" على كارته) بينما باقي الموقع (الهيدر، الفوتر...) فاضل إنجليزي زي ما هو.
- **كل المقالات بقت في مجلد واحد** بس: `content/articles/` (مفيش مجلدين `en`/`ar`
  منفصلين). اسم الملف = رابط المقال، والملف بيحدد لغته بنفسه من جوه.
- **الموقع بقى ياخد عرض الشاشة كامل** على اللابتوب (مش عمود ضيق في النص).
- ضفت **24 مقال** إجمالي: الـ 6 التجريبيين الأصليين + **18 مقال حقيقي** جبتهم من
  مجلدين عندك (`Embedded_Linux/dotfiles-linux` و `Embedded_Linux/Embedded-Linux-Notes`)
  ونضّفتهم وحطيت لهم صور/وصف/تصنيفات مناسبة.
- ضفت **LinkedIn** بتاعك لصفحة `/about`.

> ⚠️ حاولت أجيب بوستاتك الأخيرة من LinkedIn زي ما طلبت، لكن LinkedIn بيمنع أي أداة
> آلية من قراءة البوستات من غير تسجيل دخول (حماية ضد الـ scraping). لو عايز بوست
> معيّن يتحول لمقال أو سؤال وجواب، الصقلي نص البوست هنا في المحادثة وأنا أظبطه.

---

## 🖥️ كيف تشغّل الموقع على جهازك

افتح الطرفية (Terminal) ثم:

```bash
cd /media/Local-Disk2/My_Projects/Linux-website
npm install      # مرة واحدة فقط (أو عند تحديث المكتبات)
npm run dev      # يشغّل الموقع محلياً
```

ثم افتح المتصفح على: **http://localhost:3000**

> الدومين النهائي للموقع (بعد النشر في المرحلة 6): **https://shehaweyblog.vercel.app**

لإيقاف الموقع: اضغط `Ctrl + C` في الطرفية.

لبناء نسخة الإنتاج ثم تشغيلها (اختياري، للتجربة):

```bash
npm run build
npm start
```

---

## ⚠️ مهم جداً: أين يوجد المشروع فعلياً (اقرأ هذا)

قرص `‎/media/Local-Disk2‎` عندك من نوع **NTFS** (قرص ويندوز). هذا النوع من الأقراص
**لا يستطيع بناء مشاريع Next.js** بسبب طريقة تعامله مع الملفات في الذاكرة (يسبّب توقف
مفاجئ في البناء اسمه SIGBUS).

لذلك تم نقل المشروع فعلياً إلى قرص لينكس (ext4) في مجلد منزلك:

```
المسار الحقيقي:   /home/abdallah-shehawey/My_Projects/Linux-website
المسار المختصر:   /media/Local-Disk2/My_Projects/Linux-website  →  (اختصار symlink يشير للمسار الحقيقي)
```

**ماذا يعني هذا لك عملياً؟**

- تستطيع الاستمرار في استخدام نفس المسار القديم `‎/media/Local-Disk2/My_Projects/Linux-website‎`
  في المحرّر والطرفية كأن شيئاً لم يتغيّر — لأنه صار اختصاراً يفتح المسار الحقيقي تلقائياً.
- **git و GitHub و Vercel كلها تعمل بشكل طبيعي** ولم يتغيّر أي شيء فيها (نفس الريبو، نفس الرابط).
- لو أردت يوماً حذف المشروع أو نسخه، تذكّر أن الملفات الحقيقية في
  `‎/home/abdallah-shehawey/My_Projects/Linux-website‎`.
- **لا تنقل المشروع يدوياً إلى قرص `Local-Disk` أو `Local-Disk2`** (فكلاهما NTFS) وإلا سيتوقف
  البناء. أبقِه على قرص ext4 (المنزل).

> لو نقلت جهازك لتوزيعة/جهاز فيه القرص من نوع ext4 أصلاً، تختفي هذه المشكلة تماماً.

---

## 📱 تثبيت الموقع كتطبيق (PWA)

بعد نشر الموقع على الإنترنت (المرحلة 6)، يظهر في المتصفح (Chrome / Edge / Android)
خيار **«تثبيت التطبيق / Install app»**، وعلى آيفون من زر المشاركة → **«إضافة إلى الشاشة الرئيسية»**.
عندها يفتح الموقع بأيقونته الخضراء الخاصة كأنه تطبيق مستقل بدون شريط المتصفح.

---

## ✅ ما الذي تم في المرحلة 3 (Supabase والمصادقة)

- الـ **schema** كامل (جداول `profiles`, `questions`, `answers`, `notifications`,
  `question_upvotes`) + الـ **view** الخاصة بمنطق "السؤال المجهول" + كل سياسات
  **RLS** + trigger بينشئ profile تلقائياً أول ما حد يسجّل دخول — كل ده في ملف
  واحد: `supabase/migrations/0001_init.sql`.
- صفحة **`/login`**: تسجيل دخول بـ GitHub، Google، أو إيميل (رابط سحري
  magic link — من غير باسورد).
- صفحة **`/me`**: بيانات حسابك الأساسية + زرار تسجيل خروج.
- الشريط العلوي بقى يعرض **"Log in"** لو مش مسجّل، أو **"My account"** لو مسجّل.
- اختبار آلي (`npm run test:rls`) بيتأكد إن السؤال "المجهول" فعلاً بيختفي فيه اسم
  صاحبه لما حد بره الموقع يقرأه — ده شرط أساسي من متطلبات الأمان.

### ⚠️ محتاج منك 5 خطوات يدوية عشان ده يشتغل فعلياً

**1) تنفيذ ملف قاعدة البيانات (مرة واحدة فقط):**

1. ادخل **[supabase.com](https://supabase.com)** وسجّل دخول بحساب مشروعك.
2. من القائمة الجانبية افتح **SQL Editor**.
3. اضغط **New query**.
4. افتح الملف `supabase/migrations/0001_init.sql` من مجلد المشروع، انسخ **كل
   محتواه**، الصقه في محرر SQL في Supabase، واضغط **Run**.
5. المفروض تشوف رسالة نجاح. لو ظهر أي error، ابعتهولي وأنا أصلحه.
6. كرر نفس الخطوات مرة تانية بملف `supabase/migrations/0002_avatar_storage.sql`
   (بيعمل bucket اسمه `avatars` لصور البروفايل + الصلاحيات بتاعته).
7. وبعده كمان `supabase/migrations/0003_profile_onboarding.sql` (بيضيف عمود
   `onboarded` — ده اللي بيخلي أول تسجيل دخول لأي حد يوديه على صفحة
   `/welcome` يراجع/يعدّل اسمه ويوزره وصورته قبل ما يكمل، بدل ما يتحفظوا
   على طول من غير ما يشوفهم).
8. وأخيرًا `supabase/migrations/0004_profile_social_links.sql` (بيضيف عمود
   `social_links` — يقدر أي مستخدم يضيف منه لينكات حساباته من `/me`، بأي
   عدد وأي منصة، حتى لو أكتر من حساب لنفس المنصة).

**2) تفعيل تسجيل الدخول بـ GitHub:**

1. في Supabase: **Authentication → Providers → GitHub**، وانسخ الـ **Callback
   URL** الظاهر هناك (شكله `https://<project-ref>.supabase.co/auth/v1/callback`).
2. اذهب لـ **github.com → Settings → Developer settings → OAuth Apps → New
   OAuth App**.
3. **Homepage URL**: `http://localhost:3000` (وبعد النشر: `https://shehaweyblog.vercel.app`).
4. **Authorization callback URL**: الصق الـ Callback URL اللي نسخته من Supabase.
5. بعد الإنشاء، انسخ **Client ID** واعمل **Generate a new client secret** وانسخه.
6. ارجع لصفحة GitHub Provider في Supabase، الصق الـ Client ID والـ Secret،
   وفعّل المفتاح (Enable).

**3) تفعيل تسجيل الدخول بـ Google:**

1. ادخل **[console.cloud.google.com](https://console.cloud.google.com)** →
   أنشئ مشروع جديد (أو استخدم موجود).
2. **APIs & Services → OAuth consent screen** → اختر **External** → املأ اسم
   التطبيق وإيميلك.
3. **APIs & Services → Credentials → Create Credentials → OAuth client ID**
   → نوع التطبيق **Web application**.
4. في **Authorized redirect URIs** الصق نفس الـ Callback URL بتاع Supabase
   (من الخطوة اللي فاتت).
5. انسخ **Client ID** و **Client Secret**، وحطهم في **Authentication → Providers
   → Google** في Supabase، وفعّل المفتاح.

**4) خليك أنت الأدمن (بعد أول تسجيل دخول ليك في الموقع):**

1. جرّب تسجّل دخول من `/login` على `http://localhost:3000` (بأي طريقة: GitHub،
   Google، أو إيميل).
2. ارجع لـ Supabase → **SQL Editor** ونفّذ (غيّر `اسم_المستخدم` باسم المستخدم
   اللي اتسجل بيه تلقائياً — تقدر تشوفه في صفحة `/me`):

   ```sql
   update public.profiles set role = 'admin' where username = 'اسم_المستخدم';
   ```

بعد الخطوات دي، رجّع افتح `npm run dev` وجرّب تسجّل دخول وتشوف صفحة `/me`.

---

## ✅ ما الذي تم في المراحل 4 و5 و6 (نظام الأسئلة كامل + الأدمن + الإطلاق)

بناءً على طلبك، النظام هنا مختلف شوية عن الخطة الأصلية في نقطة واحدة مهمة:
**أي حد مسجّل دخول يقدر يجاوب على أي سؤال منشور (مش بس الأدمن)، والإجابة
بتظهر فورًا من غير مراجعة**، وأي سؤال ممكن يبقى عليه أكتر من إجابة. الأدمن
شغلته دلوقتي هي مراجعة الأسئلة الجديدة بس (نشر / رفض).

- **صفحة `/ask`**: نموذج طرح سؤال (عنوان + تفاصيل بمعاينة Markdown + لغة +
  تصنيفات + checkbox "انشر كمجهول")، فيه حد أقصى **5 أسئلة/الساعة** لكل مستخدم.
- **صفحة `/questions`**: أرشيف كل الأسئلة المنشورة، بحث + فلتر تصنيفات.
- **صفحة `/questions/[slug]`**: تفاصيل السؤال، كل الإجابات، زرار **"نفس السؤال
  عندي"**، ونموذج لإضافة إجابة (لأي حد مسجّل دخول).
- **الصفحة الرئيسية** بقت تعرض فعليًا أحدث الأسئلة المجابة.
- **صفحة `/me`**: بقى فيها كل أسئلتك (بأي حالة) + بادچ 🕶️ على أي سؤال منشور
  كمجهول + قسم إشعارات كامل (نشر سؤالك، حد جاوب عليه، إلخ).
- **لوحة الأدمن `/admin/questions`**: بتظهر تلقائيًا كرابط "Admin" في الشريط
  العلوي ليك أنت بس (لأن حسابك أدمن بالفعل). فيها كل الأسئلة اللي لسه تحت
  المراجعة، وزرارين **نشر** و**رفض** لكل واحد (الأسئلة المرفوضة بتتحفظ ومش
  بتتمسح نهائي، زي ما الأمان يتطلب).
- **SEO**: `sitemap.xml` و`robots.txt` تلقائيين، بيانات `Article`/`QAPage`
  المنظّمة (structured data) لجوجل، صور مشاركة (OG images) تتولّد تلقائيًا لكل
  مقال وسؤال.
- **إحصائيات زوار محترمة للخصوصية**: Vercel Analytics (بدون أي تتبع شخصي،
  وبدون Google Analytics).
- **صفحة خطأ عامة** (لو حصل عطل غير متوقع) بنفس روح صفحة الـ 404 الموجودة.
- **أضفنا الأسئلة الشائعة اللي كنت كتبتها** (Dual boot، أفضل توزيعة للمبتدئين،
  إلخ) كأسئلة حقيقية منشورة ومُجاب عليها منك أنت، بتصنيف `faq` — تلاقيها في
  `/questions?tag=faq`.
- **اسم وصورة كاتب المقال بقوا من قاعدة البيانات مش ثابتين في الكود**: أضفنا
  حقل جديد اختياري في أول أي ملف مقال اسمه `author:` وقيمته username بتاعك
  (`abdallah-shehawey`) — الموقع بياخد الاسم والصورة تلقائيًا من حسابك على
  الموقع (نفس الصورة اللي في `/me`)، فلو غيّرت صورتك يوم ما، كل الـ 21 مقال
  الموجودين هيتحدثوا لوحدهم من غير ما تلمسهم. **كل الـ 21 مقال الموجودين
  دلوقتي اتضاف لهم الحقل ده تلقائيًا** وبقى شكل الكارت بتاعهم فيه صورة صغيرة
  واسمك تحت. لو مكتبتش الحقل ده في مقال جديد، هيفضل يعرض اسمك الثابت زي ما
  كان قبل كده — مفيش أي كسر.
- **صفحة `/about` بقت حقيقية**: نفس صورتك واسمك من قاعدة البيانات (زي المقالات)،
  نبذة عنك مأخوذة من موقع البورتفوليو بتاعك (`abdallahshehawey.vercel.app`)،
  وكل روابط التواصل مأخوذة من صفحة Linkora بتاعتك
  (`linkora-rouge.vercel.app/abdallahshehawey`): Portfolio, GitHub, LinkedIn,
  X, Instagram, Telegram, WhatsApp, Email، وزرار **Facebook** بيفتح قايمة فيها
  الحسابين بتوعك (الأساسي و"For Fun") بدل ما يودّي لواحد بس.
- **بحث في المقالات**: فوق صفحة `/articles` دلوقتي فيه خانة بحث — بتدوّر في
  العنوان والتصنيفات **وجوه نص المقال نفسه**، وتفتح المقال المطابق عادي زي أي
  رابط.
- **ترتيب المقالات والأسئلة بإيدك (Drag & Drop)**: طلبت إن الترتيب يبقى زرار
  عادي فوق `/articles` مش صفحة منفصلة — بالظبط كده دلوقتي. زرار **"Reorder"**
  فوق صفحة `/articles` (وبرضو فوق `/questions`) بيظهر ليك أنت بس (الأدمن)،
  ولما تدوس عليه تقدر **تمسك أي كارت وتسحبه (drag) وتحطه في مكانه اللي عايزه**.
  الترتيب ده بيتحفظ فورًا وهو اللي **بيظهر للعامة** كلهم — في الصفحة الرئيسية
  وفي صفحتي `/articles` و`/questions` نفسهم. أي مقال/سؤال جديد لسه ما رتبتوش
  بيدخل تلقائيًا في الآخر بترتيب التاريخ العادي، لحد ما تحرّكه إنت بإيدك.
- **صفحة عامة لكل مستخدم `/u/username`**: أي حد يظهر اسمه في الموقع (سؤال،
  إجابة، كاتب مقال) بقى اسمه وصورته **قابلين للضغط** ويودّوك لصفحته العامة —
  فيها صورته، اسمه، روابط تواصله (لو حاطط أي)، وكل أسئلته وإجاباته العامة.
  **حسابك إنت بالذات (`abdallah-shehawey`) استثناء واحد بسيط**: صفحتك مش
  بتعرض قسم "الأسئلة اللي سألتها" لأن أي سؤال بتعمله إنت أصلاً بيكون عشان
  تكتبله إجابة (زي الأسئلة الشائعة) مش سؤال حقيقي ليك — بس قسم "الإجابات"
  بتاعك بيفضل ظاهر عادي.
- **رد على إجابة (Reply)**: تحت أي إجابة دلوقتي فيه زرار **"Reply"** صغير — أي
  حد مسجّل دخول يقدر يعلّق على إجابة حد، وصاحب الإجابة ياخد إشعار إن حد ردّ
  عليه.
- **أيقونة إشعارات 🔔 حقيقية** في الشريط العلوي (بدل النقطة الصغيرة اللي كانت
  على "My account")، وبتديك رقم الإشعارات اللي لسه ما قريتهاش، وبتودّيك على
  طول لقسم الإشعارات في `/me`.
- **إشعار ليك إنت بس (كأدمن) لما يجيلك سؤال جديد محتاج مراجعة** — عشان تعرف
  إن فيه حاجة محتاجة نشر أو رفض من غير ما تفتح `/admin/questions` كل شوية.
- **إيميل نسخة من كل إشعار** (لك ولأي مستخدم) — تفاصيل التفعيل في القسم اللي
  بعد الخطوات اليدوية دي مباشرة.

### ⚠️ خطوات يدوية **مطلوبة الآن** عشان كل ده يشتغل

**نفّذ ملفات قاعدة البيانات الجديدة دي بالترتيب** (زي ما عملت مع الملفات اللي قبلهم):

1. ادخل **Supabase → SQL Editor → New query**.
2. افتح الملف `supabase/migrations/0005_multi_answer_and_notifications.sql`
   من مجلد المشروع، انسخ **كل محتواه**، الصقه في المحرر، واضغط **Run**.
3. المفروض تشوف رسالة نجاح. (لو كنت شغّلت نسخة قديمة من الملف ده قبل كده
   وطلعلك error باسم `cannot change name of view column` — متقلقى، الملف
   اتصلح، شغّله تاني زي ما هو دلوقتي وهيعدي عادي.)
4. كرر بملف `supabase/migrations/0006_author_profiles.sql` (بيسمح لصفحة كل
   مقال إنها تجيب اسمك وصورتك **مباشرة من قاعدة البيانات** بدل ما تكون
   ثابتة في الكود — لو غيّرت صورة البروفايل بعدين، كل المقالات هتتحدّث لوحدها).
5. `supabase/migrations/0007_article_order.sql` (الجدول اللي بيحفظ ترتيبك
   اليدوي للمقالات — دلوقتي زرار "Reorder" نفسه فوق صفحة `/articles`، مش
   صفحة أدمن منفصلة).
6. `supabase/migrations/0008_public_profiles.sql` (بروفايل عام لأي مستخدم
   على `/u/username`، وبيخلي اسم صاحب أي سؤال قابل للضغط برضو).
7. `supabase/migrations/0009_question_order_replies_and_submit_notify.sql`
   (ترتيب الأسئلة زي المقالات بالظبط + الرد على إجابة (reply) + إشعارك انت
   كأدمن أول ما حد يبعت سؤال جديد محتاج مراجعة).
8. `supabase/migrations/0010_email_notifications.sql` (يبعتلك وللمستخدمين
   إيميل نسخة من أي إشعار — **فيها خطوة يدوية إضافية، اقرأ القسم اللي تحت
   دي مباشرة**).

### 📧 تفعيل إشعارات الإيميل (اختياري بس المفروض تعمله)

بعد ما تشغّل ملف `0010` فوق، الإيميلات مش هتشتغل لحد ما تضيف مفتاح **Resend**
(خدمة إرسال إيميلات، عندها خطة مجانية كفاية جدًا لموقع زي ده — 3000 إيميل/شهر):

1. ادخل **[resend.com](https://resend.com)** وسجّل حساب مجاني (ينفع بالـ GitHub).
2. من **API Keys** اعمل مفتاح جديد وانسخه (شكله `re_...`).
3. ارجع لـ Supabase → **SQL Editor** ونفّذ السطر ده، حط مكان `re_...` المفتاح
   الحقيقي بتاعك:

   ```sql
   select vault.create_secret('re_ضع_مفتاحك_هنا', 'resend_api_key');
   ```

4. خلاص. من غير أي حاجة تانية، من دلوقتي أي إشعار بيتبعت على الموقع (نشر
   سؤالك، حد جاوب، حد رد على إجابتك، سؤال جديد محتاج مراجعتك) هيوصلك برضو
   إيميل نسخة منه على نفس الإيميل اللي داخل بيه الموقع (Google/GitHub/إيميلك).
   المرسل هيبقى `linux-blog <onboarding@resend.dev>` (عنوان Resend التجريبي —
   لو عايز يبقى بعنوان دومينك بتاعك، لازم تتحقق من الدومين في Resend نفسه،
   ده اختياري ومش ضروري دلوقتي).
5. لو مسبتش الخطوة دي، الموقع هيفضل شغال عادي تمامًا — بس من غير إيميلات،
   الإشعار جوه الموقع نفسه (🔔 فوق) هيفضل شغال زي ما هو.

### 🌱 إضافة الأسئلة الشائعة (مرة واحدة فقط)

بعد ما تشغّل ملف الـ migration اللي فوق، شغّل السكريبت ده مرة واحدة من الطرفية
(من جوه مجلد المشروع، وبعد `npm install`):

```bash
node scripts/seed-faq.mjs
```

هيضيف الأسئلة الشائعة اللي بعتهالي زمان كأسئلة حقيقية منشورة على `/questions`،
كل واحدة بعنوانها وإجابتها، تحت تصنيف `faq`. السكريبت آمن تشغّله أكتر من مرة —
لو سؤال موجود بالفعل بيتخطّاه، فمش هيتكرر.

---

## 🔜 الخطوات اليدوية القادمة (وقت الإطلاق الفعلي)

- النشر على **Vercel** وربط الدومين (لو لسه ما عملتوش) — تأكد إن متغيرات
  البيئة الأربعة (`NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`,
  `SUPABASE_SERVICE_ROLE_KEY`, `NEXT_PUBLIC_SITE_URL`) مضافة في Vercel
  (Project Settings → Environment Variables)، مش بس في `.env.local` عندك.
- بعد ما يبقى عندك دومين نهائي: ارجع لـ Supabase → **Authentication → URL
  Configuration** وحدّث الـ Site URL، وحدّث Callback URLs بتاعة GitHub/Google
  OAuth Apps لو اختلفت.
- فعّل **Vercel Analytics** من تبويب Analytics في مشروعك على Vercel (ضغطة زر
  واحدة) — الكود بتاعه متظبط جاهز في الموقع، محتاج بس تفعيله من هناك.
- افحص `/admin/questions` بشكل دوري للأسئلة الجديدة اللي محتاجة مراجعة.
